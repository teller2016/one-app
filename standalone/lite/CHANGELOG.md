# One App Lite 변경 이력

받는 사람이 읽는 문서다 — **무엇이 달라졌는지**를 사용자 말로 쓴다(내부 구현어·파일 이름 금지).

**언제 쓰나** — 변경을 **만드는 커밋에서** `## Unreleased` 아래에 한 줄 적는다(`/commit` 이 챙긴다).
배포할 때 `/release` 가 `## Unreleased` 를 `## x.y.z — 날짜` 로 **찍고**, 그 절을 릴리스 노트로 올리며, 파일도 배포 리포에 함께 올린다.
그래서 배포 시점에 기억을 더듬어 쓰는 일이 없다.

**버전 `a.b.c` 는 이렇게 올린다** — Unreleased 항목의 표기를 보고 `/release` 가 정한다.

| 자리 | 언제 | 표기 |
|------|------|------|
| **a** (major) | 받는 사람이 **다시 설정**해야 할 때 — 저장 위치·실행 파일 이름이 바뀌어 계정을 다시 넣는 등 | `[주의]` 항목이 있으면 |
| **b** (minor) | **할 수 있는 일이 늘거나 달라질 때** — 새 화면·새 기능·기존 동작 변경 | `[추가]` 또는 `[변경]` 항목이 있으면 |
| **c** (patch) | 하는 일은 같고 **더 잘 되게** — 버그 수정·문구·속도 | `[수정]` `[개선]` 만 있으면 |

항목 표기: `[추가]` 새 기능 · `[변경]` 기존 동작이 달라짐 · `[개선]` 같은 일이 더 잘 됨 · `[수정]` 버그 · `[주의]` 받는 사람이 해야 할 일

---

## Unreleased

## 2.2.0 — 2026-09-04

- [추가] 티켓 보고에서 **레이블로 조회**할 수 있습니다. 조회 줄의 **레이블** 칸을 열면 검색창이 있어 `09` 처럼 일부만 쳐도 `26/09/17_운영배포` 가 걸리고, 여러 개를 골라 한 번에 조회합니다. 고른 레이블은 아래에 칩으로 보이고 칩을 누르면 빠집니다. 레이블을 고르면 기간 제한이 자동으로 꺼집니다 — 배포 레이블은 티켓을 만든 달과 상관이 없어서, 기간을 걸면 8월에 만들어진 10월 배포 티켓이 빠졌습니다(전체 조회 후 손으로 거를 필요가 없어집니다).
- [변경] 결과 아래에 있던 레이블 필터는 위 조회 조건으로 올라갔습니다. 선택지는 **고른 프로젝트가 쓰는 레이블**이고 **최신 날짜부터** 나옵니다.
- [추가] 결과 정렬에 **레이블별**이 생겼습니다 — 최신 배포 레이블부터 묶어 보여줍니다(보고서를 쓰는 순서). 고른 레이블·칩·복사 결과도 같은 최신순으로 맞췄습니다.

## 2.1.1 — 2026-09-03

- [수정] 환경설정의 **Jira 주소** 칸에 티켓 주소(`…/browse/ABC-123`)나 보드 주소를 그대로 붙여넣어도 티켓 보고가 동작합니다. 예전에는 조회할 때 `Unexpected token '<' …` 같은 알 수 없는 오류가 나고, 무엇이 잘못됐는지 알 수 없었습니다.
- [개선] Jira 가 조회 결과 대신 웹페이지를 돌려주면 **무엇을 확인해야 하는지** 알려 줍니다(주소 확인 안내).
- [수정] 새 버전 알림·오류 알림의 맨 왼쪽 아이콘이 글자보다 위로 올라가 있던 것을 줄 중앙에 맞췄습니다.

## 2.1.0 — 2026-09-03

- [추가] 새 버전이 나오면 앱 안에서 **[지금 업데이트]** 한 번으로 내려받아 설치하고 자동으로 다시 시작합니다 (Windows · Mac). 설정과 계정은 그대로 유지됩니다.
- [개선] 업데이트로 받은 파일에는 검역 표시가 붙지 않아, 업데이트 때는 Mac 의 `xattr` 명령이나 Windows 의 "PC 보호" 경고 없이 바로 실행됩니다.
- [개선] 업데이트 중 내려받기 진행률을 보여주고, 받은 파일의 크기·체크섬을 검증한 뒤 설치합니다. 자동 설치가 안 되는 상황이면 받아 둔 파일로 직접 교체할 수 있게 [받은 폴더 열기]를 제공합니다.
- [개선] 제목 옆에 현재 버전이 표시되고, 환경설정 **'버전'** 그룹에서 확인·수동 업데이트가 됩니다.

## 2.0.0 — 2026-09-03

- [추가] **결재** — 야근 · 휴가신청서 · 지출결의서(개인)를 대신 작성합니다. 작성까지만 하고 [상신]은 직접 누릅니다.
- [추가] **티켓 보고** — Jira 티켓을 프로젝트·기간으로 모아 상태·담당자·레이블·유형으로 걸러내고, 원하는 형식(템플릿)으로 한 번에 복사합니다.
- [추가] 새 버전이 나오면 앱 상단에 알림이 뜨고 [받기]로 내려받는 페이지가 열립니다.
