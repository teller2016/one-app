# One App Lite 변경 이력

받는 사람이 읽는 문서다 — **무엇이 달라졌는지**를 사용자 말로 쓴다(내부 구현어·파일 이름 금지).
`/release` 가 배포할 때 이 파일의 해당 버전 항목을 릴리스 노트로 올리고, 파일 자체도 배포 리포에 함께 올린다.

**버전 `a.b.c` 는 이렇게 올린다** — 항목의 표기를 보고 `/release` 가 정한다.

| 자리 | 언제 | 표기 |
|------|------|------|
| **a** (major) | 받는 사람이 **다시 설정**해야 할 때 — 저장 위치·실행 파일 이름이 바뀌어 계정을 다시 넣는 등 | `[주의]` 항목이 있으면 |
| **b** (minor) | **할 수 있는 일이 늘거나 달라질 때** — 새 화면·새 기능·기존 동작 변경 | `[추가]` 또는 `[변경]` 항목이 있으면 |
| **c** (patch) | 하는 일은 같고 **더 잘 되게** — 버그 수정·문구·속도 | `[수정]` `[개선]` 만 있으면 |

항목 표기: `[추가]` 새 기능 · `[변경]` 기존 동작이 달라짐 · `[개선]` 같은 일이 더 잘 됨 · `[수정]` 버그 · `[주의]` 받는 사람이 해야 할 일

---

## 2.1.0 — 2026-09-03

- [추가] 새 버전이 나오면 앱 안에서 **[지금 업데이트]** 한 번으로 내려받아 설치하고 자동으로 다시 시작합니다 (Windows · Mac). 설정과 계정은 그대로 유지됩니다.
- [개선] 업데이트로 받은 파일에는 검역 표시가 붙지 않아, 업데이트 때는 Mac 의 `xattr` 명령이나 Windows 의 "PC 보호" 경고 없이 바로 실행됩니다.
- [개선] 업데이트 중 내려받기 진행률을 보여주고, 받은 파일의 크기·체크섬을 검증한 뒤 설치합니다. 자동 설치가 안 되는 상황이면 받아 둔 파일로 직접 교체할 수 있게 [받은 폴더 열기]를 제공합니다.
- [개선] 제목 옆에 현재 버전이 표시되고, 환경설정 **'버전'** 그룹에서 확인·수동 업데이트가 됩니다.

## 2.0.0 — 2026-09-03

- [추가] **결재** — 야근 · 휴가신청서 · 지출결의서(개인)를 대신 작성합니다. 작성까지만 하고 [상신]은 직접 누릅니다.
- [추가] **티켓 보고** — Jira 티켓을 프로젝트·기간으로 모아 상태·담당자·레이블·유형으로 걸러내고, 원하는 형식(템플릿)으로 한 번에 복사합니다.
- [추가] 새 버전이 나오면 앱 상단에 알림이 뜨고 [받기]로 내려받는 페이지가 열립니다.
