/**
 * 머지 직후 '충돌 재검사' 창 — Gitea 는 base 브랜치가 갱신되면(= 그 브랜치로 PR 이
 * 머지되면) 그 base 를 향한 **열린 PR 전부**를 다시 충돌 검사하고, 그동안 API 의
 * `mergeable` 을 false 로 준다. PR 응답에는 상태 구분 필드가 없어(1.25.4 실측:
 * `mergeable` 불리언 하나뿐) **재검사 중과 실제 충돌을 구분할 수 없다**.
 *
 * 그래서 머지 성공 후 이 창 동안은 false 를 '충돌'로 단정하지 않고 모름(undefined)으로
 * 낮춰 두고, 짧은 주기로 재조회해 확정한다. 창이 만료되도록 false 로 남은 PR 은
 * 실제 충돌로 본다.
 */

/** 재검사 창 길이 — 이 시간이 지나면 남은 false 를 실제 충돌로 확정한다 */
export const RECHECK_MS = 30_000;

/** 재검사 창 동안의 재조회 주기 */
export const RECHECK_POLL_MS = 3_000;
